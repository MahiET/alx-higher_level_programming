#!/usr/bin/python3

def print_reversed_list_integer(my_list=[]):
    """
    prints the reverse of a given list
    """
    if 0 <= idx < len(my_list):
        my_list[idx] = element
        return(my_list)
